# HANDOFF — 2026-08-07 · read after `INDEX.md`, before anything else
**Registration 2026-08-25, 09:00–17:00 KST. 18 days out.** Iden returns ~2026-08-10.

Supersedes `HANDOFF_2026-08-06.md` entirely. That file's weights are all stale.
This session rewrote most of the model; `MODEL.md` is the cross-section, `RULES.md` R129–R166
is the evidence. **Do not re-derive anything below — it is all logged with quotes.**

---

## 1 · WHERE THE DECISION ACTUALLY STANDS

The 5000 collapses to **one question worth 0.73 points** (R161):

| | best | what it postpones |
|---|---|---|
| **family A** | 21.80 | **Chinese** |
| **family B** | 21.07 | **QRM입문** |

**On the modelled terms these are a tie** — 0.73 is inside the model's own bracket width, and
R160 proved the trip-curve exponent cannot separate them (both are 월금, the term cancels
exactly). Every four-year term is symmetric too: both leave 2 items for the one 국제 Spring,
both deferred items are chart-year 1 (R165).

### ⚠️ But three unmodelled arguments all point at **family B**
1. **Scarcity** (R165) — Chinese is oversubscribed 5 of 7 semesters, 2-seat 분반, winning bids
   14–28 mileage. QRM입문 has 78 seats and has **never** been oversubscribed.
2. **Access timing** (R166) — Beginning Chinese/Japanese are *meant for freshmen*; this Fall
   Iden draws on freshman seats via 대기순번제. From 2학년 he bids mileage against those same
   2-seat sections. **The easy language is cheapest to obtain right now.**
3. **GPA** (R153/R166) — the fallback to a deferred easy language is a *hard* 언어와표현 course.
   Taken in Fall 2026 that is a risk in the one semester gating double-major admission; taken
   later it is not.

⇒ **Working recommendation: take the language now, defer QRM입문 — family B.**
Not because it scores higher. It scores 0.73 lower and wins on three things the score cannot
see. **Say this to Iden explicitly; do not present the 21.80 as "the answer".**

---

## 2 · THE PROJECT UPDATE IDEN IS ASKING FOR — scope

He said: *"We are going to prepare for a big project update encompassing these different
factors."* Four factors surfaced today, **in this priority order**:

### A. RISK — the only one that has already reversed an answer
Competition and availability. Materials exist (`mileage_history.json`, per-학년 quotas) but
nothing is built. ⚠️ Two different regimes, do not conflate (R130 vs R165):
- **1학년 (Fall 2026)**: 대기순번제, freshman seats, mileage 배율 says **nothing** about him.
- **2학년+ (everything deferred)**: he **is** a bidder; the same numbers describe his race.

### B. DIFFICULTY — decision-relevant twice today, representable zero times
The model has **no difficulty axis at all** (R153). The early arm of the 학년 gap is silently
substituting for one. Needed for: the GPA loop, and the easy/hard language tiers (R166).
⚠️ No difficulty data exists for these courses. Getting it may mean asking Iden per-course,
which **R137 forbids** — so the mechanism has to come first.

### C. AVAILABILITY ACROSS TIME — term × campus × language × dept × time
R159 (ECO1101 is 월수 every Fall but has offered 화목 in both recorded Springs) and R164
(ECO1101's only English 신촌 section is 월1,2/수2 — Monday *and* a 9am) both changed decisions,
and both were hand-queried. The four-year layer needs per-course availability, not "does this
course exist".

### D. QUOTA ENFORCEMENT
Pools are **priced** (R149) but not **enforced** — nothing checks he reaches 18 ME / 6 Seminar /
15 free-elective credits by graduation. Needs the four-year optimiser.

### Also outstanding, smaller
- Widen `LANG` from 2 courses to the **10 level-1 courses** (R166), with an easy/hard tier —
  **blocked on B**.
- Swap R117's fitted deferral table for the derived one — **blocked on A** (R156).
- Re-run the 9 remaining 2-deferral pairs (R132).

---

## 3 · 📅 CALENDAR — the only hard constraints

| date | what |
|---|---|
| 8.10–8.11 | 2학년+ mileage round. Iden not involved. |
| 8.12 16:00 | mileage results published |
| 8.13–8.14 | 2학년+ 추가수강신청 |
| **8.15 – 8.24** | ⚠️ **the window.** Re-run `fetch_mileage.py`. A per-학년 quota of **0** makes registration *impossible*, not merely hard (R134). Then build Plan B click order from the **12 family representatives** (R161), not the 5000. |
| **8.25 09:00** | Registration. 대기순번제, 정원미달 only. |

**Cleanup and modelling are not on the critical path. That window is.**

---

## 4 · WHAT NOT TO REPEAT — this session's failure modes

Every one of these cost time and Iden caught most of them.

1. **Concluding before the inputs are complete.** I gave three different verdicts on QRM입문 in
   one session (R143 → R144 → R148), each driven by data I had not yet looked at. R143 even
   flagged its own missing input *and drew the conclusion anyway*.
2. **Reading a summary line as if it were the output.** Twice (R155). `rank3.py` prints the best
   score and the deferral counts; **neither is a statement about the top 50.**
3. **Writing documentation ahead of the code.** Three divergences today, all the same direction
   — the document claimed more than the code delivered (R157, R162).
4. **Disabling instead of deleting.** A superseded value left resident in the tree looks
   authoritative (R157/R162). Delete or make it raise.
5. **Eliciting from output, or in increments.** R136/R141: ask about *states*, compute the
   differences yourself. Iden reasons well about "which semester would I rather have" and
   badly about "what is the marginal day worth" — that second one is the arithmetic half, and
   it is not his job.
6. **Treating one of Iden's own decisions as a rule.** D-3 restricted the language pool to 2
   courses for three days; it was a preference (R166). **Check whether a constraint is his
   choice or the university's before letting it narrow a search.**

---

## 5 · CODE TRAPS — in `INDEX.md`, repeated because they bite silently
1. `rank2.py` **cannot be reordered or reformatted above** `    heap = []; cnt=[0]` — `rank3`
   `exec`s its source text up to that literal string.
2. `rank.py` looks legacy; its functions are disabled but **every weight constant lives there**.
3. `defer2_check.py` hard-codes the current optimum and went stale **twice in one day**.

---

## 6 · STATE
`test_weights.py` **23/23**. Pipeline runs clean end to end. Folder 23 MB, 79 MB archived with
`archive/README.md` explaining what was kept and why. `TOP50.html` is current and now shows
professors, full 학정번호-분반, and **two distinct kinds of alternate** (R163) — *same course,
different 분반, same time* (a zero-cost registration-day fallback) versus *different course,
same slot, same score*.

**Every schedule constant traces to something Iden said. The requirement side is half-built.
Availability is not modelled at all, and that is the gap with a date on it.**
